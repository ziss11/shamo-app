<resources>

    <style name="Base.Theme.ShamoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/black1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:includeFontPadding">false</item>
        <item name="floatingActionButtonStyle">@style/FabStyle</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Base.Theme.ShamoApp</item>
        <item name="windowSplashScreenBackground">@color/black1</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_logo</item>
    </style>
</resources>